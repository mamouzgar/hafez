% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddr.R
\name{hafez_dDR}
\alias{hafez_dDR}
\title{hafez_dDR}
\usage{
hafez_dDR(
  ts_input = NULL,
  cells = NULL,
  pseudotime_column = NULL,
  groups = NULL,
  method = c("w2", "dtw"),
  approach = c("density", "expr", "other"),
  dims = 2L,
  k = NULL,
  m = 64L,
  min_n = 30L,
  weight_by_n = TRUE,
  seed = 0,
  type = "partitional",
  distance = "dtw_basic",
  normalize = TRUE,
  ...
)
}
\arguments{
\item{ts_input}{long data frame of curves with columns \code{x} (pseudotime) and \code{y} (density or expression) plus the \code{groups} columns. Used when raw cells are unavailable; ignored if \code{cells} is supplied.}

\item{cells}{cell-level data frame. Preferred for \code{method = "w2"}: the quantile embedding is computed directly from the values, with no kernel density estimate, bandwidth or grid.}

\item{pseudotime_column}{name of the pseudotime column in \code{cells}.}

\item{groups}{character vector of grouping column names.}

\item{method}{\code{"w2"} for Wasserstein PCA (default) or \code{"dtw"} for dynamic time warping plus classical MDS.}

\item{approach}{\code{'density'}, \code{'expr'} or \code{'other'}, describing \code{ts_input}.}

\item{dims}{number of dDR dimensions to return.}

\item{k}{number of clusters. \code{NULL} (default) means no clustering.}

\item{m}{quantile grid size for \code{method = "w2"}. The quantile function is smooth, so 64 is usually indistinguishable from 512 and much cheaper.}

\item{min_n}{minimum cells per group when embedding from \code{cells}.}

\item{weight_by_n}{weight groups by cell count (only when \code{cells} is given).}

\item{seed}{RNG seed for clustering and for DTW.}

\item{type, distance, normalize}{passed to \code{\link[dtwclust]{tsclust}} for \code{method = "dtw"}.}

\item{...}{further arguments for the chosen back-end.}

}
\value{
A list with \code{dDR} (a data frame of coordinates, the grouping
columns, and \code{dDR_cluster} if \code{k} was given), \code{method},
\code{var_explained} (\code{w2} only), \code{Q} or \code{distmat}, and
\code{centroids}: Wasserstein barycentres for \code{w2}, \pkg{dtwclust}
centroids for \code{dtw}.
}
\description{
Density-based dimensionality reduction of pseudotime cell density
distributions, with a choice of geometry.

\strong{\code{method = "w2"} (the default)} embeds each group as a quantile
function and runs Wasserstein PCA. Because the quantile map is an isometry for
the 2-Wasserstein distance, this is exact: a true metric, no negative
eigenvalues, a genuine variance-explained figure, and axes in pseudotime units.
It also never forms the n x n distance matrix.

\strong{\code{method = "dtw"}} reproduces the previous behaviour: DTW
distances via \pkg{dtwclust}, then classical MDS. Retained so the two can be
compared on identical input. Note that DTW is built to be invariant to shifts
along the x axis, which on a normalised pseudotime axis is usually the effect
of interest, and that it is not a metric, so \code{cmdscale} needs
\code{add = TRUE} to absorb negative eigenvalues; a warning reports how much
eigenvalue mass that involves.

Clustering is \strong{off by default}. On the data this was developed against
the structure is a continuous gradient rather than discrete groups: a gap
statistic selected k = 1 and the first component carried about 91 percent of
the variance. Treating the leading score as a continuous distributional
phenotype is usually more informative than a partition. Supply \code{k} to opt
in.
}
\seealso{
\code{\link{hafez_tsvz}} for the original DTW-only interface.
}
